baseURL = "https://jennyKuopd.github.io/"
languageCode = "zh-tw"
title = "Jenny｜Product designer"
theme = "hugo-lodi-theme"
# googleAnalytics = "UA-XXXXXXXX-Y"

[params]
    # head theme color
    theme_color = ""

    # head description
    # This is what appears on the preview when posting this site on Facebook for example
    description = ""

    # head photo
    # This is what appears on the preview when posting this site on Facebook for example
    description_photo = "/static/img/Jennyphoto.jpeg"

    # favicon image (512x512px), place this in the static/img folder
    favicon = "/static/img/favicon.jpg"

    # logo image (204x204px), place this in the static/img folder
    # this appears on the top left of the navigation
    logo = "/static/img/logo.png"

    # Email
    # email = ""

    # Facebook username
    # facebook = ""

    # Twitter username
    # twitter = "asdf"

    # Dribbble username
    # dribbble = ""

    # LinkedIn URL
    # linkedin = ""

    # Medium username
    # medium = "asdf"

    # Github username
    # github = ""

    # Google Scholar URL
    # googlescholar = ""

[params.nav]
    enable = true

[params.hero]
    enable = true

    # Place this image in the static/img folder
    avatar = "avatar.jpg"
    subtitle = "Hi! I'm Jenny, a UX/UI Designer and from Taiwan."

    # Each element in this array makes a new <p> tag
    # You can write this text in markdown
    hero_right = [
    "**This is a bold paragraph** and it's amazing.",
    "This is my [GitHub](https://jennykuopd.github.io/). It's pretty cool.",
    "I also do a lot of coding if you didn't already know.",
    "I study at National Yunlin University and I expect to graduate in 2019.",
    "Please enjoy the rest of my website."]

    # Link to your resume, place your PDF in the /static folder
    resume = "resume.pdf"

[params.process]
    enable = true
    title = "My Process"
    subtitle = "Full Process Design"

[params.case_studies]
    enable = true
    title = "Case Studies"
    subtitle = "Selected Works"

[params.footer]
    enable = true
